"""Web UI package: FastAPI server + static frontend.

Run with ``python -m multi_agent_cad.web`` (delegates to
``multi_agent_cad.web.server:main``). Single-user, trusted-network only —
the pipeline executes generated Python server-side.
"""
