# MAC (multi agent cad) — pip requirements
#
# Recommended: install via conda (environment.yml) so native C extensions
# (numpy, scipy, trimesh, rtree) come from conda-forge prebuilt wheels:
#
#     conda env create -f environment.yml
#     conda activate multi_agent_cad
#
# This file is a reference list of dependencies — do NOT run
# `pip install -r requirements.txt` directly. `aider-chat` pins
# `numpy==1.26.4` while `build123d>=0.8` requires `numpy>=2,<3`, which
# makes pure pip fail with ResolutionImpossible. See README §2 for the
# conda path (recommended) or the verified pip workaround.
# On Windows, prefer conda — native wheels are unreliable.

# ── Core scientific / math ─────────────────────────────────────────────
numpy>=1.24,<2.3
scipy>=1.10
scikit-learn>=1.3

# ── Mesh analysis ─────────────────────────────────────────────────────
trimesh>=4.0
rtree>=1.1

# ── CAD kernel ────────────────────────────────────────────────────────
build123d>=0.8          # pulls in cadquery-ocp-novtk (provides `OCP`) transitively

# ── LangGraph orchestration ───────────────────────────────────────────
langgraph>=0.2,<0.3
langgraph-checkpoint>=2.0,<3.0

# ── Data contracts ─────────────────────────────────────────────────────
pydantic>=2.5

# ── LLM clients ────────────────────────────────────────────────────────
openai>=1.20.0           # DashScope (Qwen) OpenAI-compatible endpoint
anthropic>=0.30          # optional fallback provider

# ── Autonomous code repair engine ──────────────────────────────────────
aider-chat>=0.50

# ── Dev / debug (optional) ────────────────────────────────────────────
ipython>=8.15
pytest>=7.4
