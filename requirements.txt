# MAC (multi agent cad) — pip / veFaaS requirements
#
# This file is intentionally installable with:
#
#     python -m pip install -r requirements.txt
#
# The root package declares the verified CAD, orchestration, science, mesh,
# and web dependencies. cadpy lives in this repository as a separate local
# package, so install it explicitly before the application package.
#
# aider-chat is intentionally omitted from the hosted build. The verified
# local Aider version pins NumPy 1.26.4 while build123d requires NumPy 2.x.
# The application already falls back to its direct LLM repair path whenever
# Aider is unavailable, which keeps the hosted dependency graph resolvable.

./packages/cadpy
.[mesh,science,web]
